name: tiledjsonreader
description: Simple reader to convert files .json generated from Tiled into Objects
version: 1.3.5
homepage: https://github.com/RafaelBarbosatec/tiled_json_reader

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

